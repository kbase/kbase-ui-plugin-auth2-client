---
description: Access the login buttons
disable:
  envs:
    - appdev
specs:
  - description: should work
    baseSelector: []
    tasks:
      - action: navigate
        path: login
      - subtask: plugin
      - wait: forElement
        selector:
          - type: component
            value: login-view
          - type: element
            value: signin
      - wait: forElement
        selector:
          - type: component
            value: login-view
          - - type: component
              value: signin-button
            - type: name
              value: google
      - wait: forElement
        selector:
          - type: component
            value: login-view
          - - type: component
              value: signin-button
            - type: name
              value: globus
